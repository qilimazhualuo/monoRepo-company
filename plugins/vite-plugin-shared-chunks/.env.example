# 自动扫描 main + CONSUMER_APPS 的重复依赖并打入 shared（默认开启）
# 如需额外强制共享某些包，取消下行注释（逗号分隔）
# PACKAGES=wc-ui,wc-utils

# producer 构建时扫描的子应用（相对 apps/），用于补齐 shared export
CONSUMER_APPS=sub-app

# 共享 chunk 的站点绝对路径前缀
PUBLIC_PATH=/shared/
