# 网关端口
PORT=9000

# 静态资源（仅 gateway 提供，默认 monorepo 根目录 dist，需先 yarn build）
# PUBLIC_DIR=../../dist
# PUBLIC_ENABLED=true

# 上游服务（也可通过 GATEWAY_SERVICES JSON 覆盖）
AUTH_UPSTREAM=http://127.0.0.1:9001
BASIC_UPSTREAM=http://127.0.0.1:9002
# GATEWAY_SERVICES=[{"serviceId":"auth","upstream":"http://127.0.0.1:9001"}]

# 内部接口（basic 拉取微服务列表）
INTERNAL_API_SECRET=mono-repo-internal-dev

# 跨域
CORS_ORIGIN=http://localhost:3000

# 鉴权（需与 auth 服务保持一致）
JWT_SECRET=mono-repo-dev-secret-change-me
COOKIE_NAME=mono_token
COOKIE_MAX_AGE=604800

# Redis 会话
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
SESSION_PREFIX=mono:session:

# ---------- Nacos ----------
NACOS_ENABLED=false
NACOS_SERVER_ADDR=127.0.0.1:8848
NACOS_NAMESPACE=public
NACOS_GROUP=MONO_REPO
NACOS_CONFIG_DATA_ID=gateway
NACOS_SERVICE_NAME=gateway
NACOS_SERVICE_IP=127.0.0.1
NACOS_USERNAME=admin
NACOS_PASSWORD=123zhangbei
